import { TextLimitDirective } from './text-limit.directive';

describe('TextLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new TextLimitDirective();
    expect(directive).toBeTruthy();
  });
});
